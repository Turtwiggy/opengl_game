#pragma once

// other library
#include <SDL2/SDL_keycode.h>
// #include <SDL2/SDL_events.h>
// #include <SDL2/SDL_gamecontroller.h>
// #include <SDL2/SDL_mouse.h>
// #include <glm/glm.hpp>

#include <map>
#include <utility>
#include <vector>

namespace game2d {

// bool
// key_held(SINGLETON_InputComponent& input, SDL_Scancode button);

// // void
// // new_frame(const uint64_t& frame);
// // void
// // process_key_down(const SDL_Scancode button, const Uint8 is_repeat);
// // void
// // process_key_up(const SDL_Scancode button, const Uint8 is_repeat);

// // keyboard
// [[nodiscard]] bool
// get_key_down(SDL_Scancode button);
// [[nodiscard]] bool
// get_key_up(SDL_Scancode button);
// [[nodiscard]] bool
// get_key_held(SDL_Scancode button) const;

} // namespace game2d